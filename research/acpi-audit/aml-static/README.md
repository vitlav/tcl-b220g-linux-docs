# Статический аудит AML TCL, 2026-09-10

Проверка выполнена локально, без перезагрузки TCL и без исполнения методов на оборудовании. Исходные OEM-таблицы не изменялись.

## Целостность и границы проверки

DSDT.dat: 189125 байт, длина заголовка совпадает, сумма байтов mod256=0. SHA256: `78b42f6edc265bffdb3ad1d82c0e63bd98792ab42db9238571f893eeeb06b19f`.
В сохранённой DSDT 324 объявления Method, 47 методов _STA, два _REG, по три _PS0/_PS3, 12 OperationRegion. Объявлений _INI нет. Эти числа не доказывают полноту всех возможных дополнительных SSDT.

## Методы и аппаратные обращения

Ссылки на строки относятся к исходному ../../DSDT.dsl.

| Код | Строки | Что делает | Значение для проверки |
|---|---:|---|---|
| ABD._REG | 311 | Для GenericSerialBus (9) меняет AVBL | Сам не записывает аппаратные регистры |
| GIO0._REG | 43108 | Для GPIO (8) меняет GABL | Сам не переключает питание |
| LID0._PS0/_PS3 | 45940 | Пустые методы | Не объясняют погасание экрана |
| TCPD._PS0/_PS3 | 46258 | Формирует команду D0/D3 для PEP0.FLD0 | Запись только при ABD.AVBL; требуется OEM handler |
| ECKB._PS0/_PS3 | 46387 | Меняет GIO0.LISP, GPIO32 | Запись только при GIO0.GABL; не управление дисплеем |
| GPU0._ROM | 42027 | Читает до 4KiB SystemMemory по адресу Arg2 | Не считать автоматически исполняемым при сканировании; не вызывать с произвольным адресом |
| _SB._BID/BSID/BREV | 46663 | Читает GPIO через SystemMemory без GPIO handler | Существенно для _STA: наличие pinctrl-драйвера не требуется для самого AML-чтения |
| IPA._STA | 43349 | Вызывает BREV | Возможен прямой MMIO-доступ при перечислении устройств |
| RVRM._STA | 47569 | Вызывает BREV | Аналогично; это не доказанный источник зависания |
| RFS0._STA | 43303 | BSID вызывается в условии с PLST==1 | В сохранённой таблице PLST=0xffffffff; не считать эту ветку обязательно выполненной |

Адреса board-ID совпадают с Linux sc7180.dtsi и pinctrl-sc7180.c (tile base + 0x1000 * pin, входной регистр +4):

| GPIO | Tile | ACPI OperationRegion base | Читаемый адрес |
|---:|---|---|---|
| 14 | south 0x03d00000 | 0x03d0e000 | 0x03d0e004 |
| 66 | north 0x03900000 | 0x03942000 | 0x03942004 |
| 67 | north 0x03900000 | 0x03943000 | 0x03943004 |
| 68 | north 0x03900000 | 0x03944000 | 0x03944004 |

Соответствие адресов подтверждено статически; доступность регистров в состоянии ACPI-boot не доказана. Не выполнялись devmem, GPIO-транзакции или оценка этих методов на TCL.

В ACPICA nsinit.c ветки без _INI пропускаются при обходе инициализации устройств. Однако Linux scan.c отдельно вызывает acpi_bus_get_status: отсутствие _INI не исключает выполнения _STA при дальнейшем перечислении.

## Проверка компилятором

`iasl` 20250807: повторная декомпиляция исходного DSDT.dat успешна. Компиляция сохранённого DSDT.dsl завершилась 8 Errors, 32 Warnings, 311 Remarks; полный журнал iasl.log.

- Отдельные Zero на уровне Scope (около110) мешают обратной компиляции. Свежая декомпиляция исходного AML воспроизводит их; это не ручная порча файла.
- PRTC._DEP содержит строку вместо ссылки (478).
- GPU0._CLS содержит Integer вместо Package (35057).
- HID EDPBridge и AWDZ9610X имеют длину9 вместо7/8 (44996/45957).
- Ошибки отсутствующего SIDV могут быть следствием остановки парсера выше: объявление SIDV в тексте есть.

Это нарушения/препятствия обратной компиляции, а не доказательство, что OEM AML не исполняется Windows или является причиной чёрного экрана Linux. OEM-таблицы не исправлялись и override не создавался.

## Ограничение локального acpiexec

Попытки `acpiexec -l -di -b 'tables' DSDT.dat` с обычным и hardware-reduced FADT не дали подтверждённой загрузки namespace: инструмент выводит неверные адреса/сигнатуры своих синтетических таблиц. Возврат0 не означает успешную проверку.

Минимальный контроль control.asl (только Name(TEST, One), без устройств и методов) компилируется и воспроизводит тот же дефект; см. control-exec.log. Поэтому эти сообщения не относятся к контрольной сумме TCL DSDT. Требуется исправный независимый acpiexec для последующего моделирования. Аппаратные методы в этих опытах не вызывались; использованы load-only и отключение STA/INI.

## Что остаётся

1. Получить исправный инструмент моделирования и проверить загрузку исходного AML, не подменяя реальные значения регистров выводами из симуляции.
2. Составить полный список _STA с прямыми/косвенными MMIO-чтениями и учесть условия OEM board-ID.
3. Проверить полноту набора AML-таблиц и разрешение External, особенно OEM-служб.
4. Причина чёрного экрана остаётся неизвестной. Новую загрузку нельзя считать подготовленной без проверенного способа получить логи после потери изображения.

## Продолжение: независимый интерпретатор и проверка методов

Собран немодифицированный официальный ACPICA commit `57a2afa78dbe3580e19307aedd19f32388512094`, acpiexec20260408. Команда: `make acpiexec -j8 OPT_CFLAGS=-O2`. Обычная сборка останавливалась из-за повторного определения FORTIFY_SOURCE; исходники не меняли, системный пакет не заменяли. Бинарник `/tmp/tcl-aml-static/acpica-src/generate/unix/bin/acpiexec`.

Контрольная таблица загрузилась. Исходный TCL DSDT.dat также загрузился: 1084 объекта таблицы,115устройств,11статических регионов,324метода. Двенадцатый OperationRegion находится внутри GPU0._ROM и не создавался. При инициализации данных обнаружен `AE_NOT_FOUND` для `\_SB.GPU0.AVS0`, ссылка из TZ7._TZD (DSDT.dsl46890). В сохранённой DSDT определения AVS0 нет. Это подтверждено в изолированной DSDT, но полнота возможных дополнительных таблиц пока не доказана. Проверка использует синтетический FADT инструмента, не OEM FADT TCL, и более новую ACPICA, чем ядро6.18.34. Успешная загрузка здесь не доказывает работоспособность ACPI на ноутбуке.

`board-id-method-index.json` содержит24метода с прямыми вызовами BSID/BREV/_BID; среди них19методов _STA/_SUB/_CRS. Выполнена симуляция11методов _STA (список и результаты в sta-sim-fill0.log и sta-sim-fill1.log). Все завершились с результатами0x0f, кроме RFS0=0x0b. Использованы виртуальные регионы с заполнением0 и1, НЕ реальные значения GPIO; оба варианта дали одинаковые статусы. Это не полный перебор board-ID и не доказательство безопасности MMIO на железе.

И новый acpiexec сообщает4outstanding cache allocations при завершении, включая минимальный контроль. Это отдельное диагностическое сообщение инструмента, не свидетельство дефекта TCL.

На дефект установленного acpiexec отправлено создание отдельной задачи (Отдел исследований/Система), confirmation `3520d5ca-a6e1-46c7-9014-fe9ac877ffae`, ожидает подтверждения. После создания связать с19492 и записать время; повторно не создавать.

## Все _STA и чтение живых таблиц

Все47методов _STA проверены в симуляции fill0, пятью группами из-за ограничения acpiexec1023байта на batch-команду. 46вернули Integer, BAT0._STA завершился AE_NOT_EXIST: нет GenericSerialBus handler для ECBF/IC3R. Это ожидаемое отсутствие I2C-обработчика в изолированной среде и существенная зависимость аппаратного порта, не доказательство причины чёрного экрана. Полная таблица all-sta-results.md; журналы all-sta-batch0..4.log.

В рабочем Linux boot1589e0d5-7a52-4d0b-8405-d8b498bdbbc2 EFI сообщает ACPI20=0xffffd000, область0xfff22000–0xffffdfff зарезервирована внутри SystemRAM. Ограниченное чтение36байт RSDP через /dev/mem O_RDONLY запрещено EPERM. Ни таблицы, ни MMIO прочитаны не были. read-live-acpi.py останавливается на выходе за проверенный ACPI-диапазон и исключает содержимое MSDM.

Пользователь согласен при случае снять защиту для подробного исследования. TODO: отдельный диагностический вариант рабочего DT-ядра с CONFIG_DEVMEM=y, CONFIG_STRICT_DEVMEM=n; сохранить рабочий пункт, Wi-Fi/SSH и логирование. Нынешнее ядро не изменено, сборка/перезагрузка ради этого не выполнялись. По lib/devmem_is_allowed.c запрет page_is_ram независим от iomem_is_exclusive: одного iomem=relaxed недостаточно. Разрешение относится к исследованию; не к произвольным записям MMIO.

Бага инструмента создана:19510. Связь blocks19492 ожидает подтверждения be2fbce8-f3e7-4bfe-8353-e3e0d23ebe5d.

## Порядок зависимостей Linux и ещё72метода (2026-09-10)

Важное уточнение к прямому вызову BAT0._STA в симуляторе: ядро уже учитывает этот случай. drivers/acpi/bus.c:105–109 в acpi_bus_get_status возвращает status0 без вызова AML для battery с dep_unmet. В scan.c acpi_scan_dep_init выполняется перед acpi_scan_init_status; GPIO/I2C dependencies подсчитываются даже без honor_deps. PEP0 сCIDPNP0D80 игнорируется существующим правилом Linux. При отсутствии GIO0/I2C3 батарея должна ждать зависимости, а не обязательно исполнять проблемный _STA. Не переносить AE_NOT_EXIST из прямой симуляции на причинное объяснение загрузки.

В i2c-core-base.c порядок: i2c_acpi_install_space_handler затем i2c_acpi_register_devices; последний вызывает acpi_dev_clear_dependencies. gpiolib-acpi-core.c также снимает зависимости после регистрации регионов. Возврат i2c_acpi_install_space_handler в i2c_register_adapter не проверяется: потенциальный пограничный случай при ошибке установки, но такой аппаратный отказ не подтверждён.

acpi_get_object_info (nsxfname.c) не исполняет _SUB с2015года и _STA с2018года. Поэтому наличие прямых чтений в PEP0._SUB не означает их неизбежное выполнение через стандартное получение идентификаторов Linux. Отдельные _STA и _CRS остаются другими путями.

В симуляции fill0 выполнены все72объявленных метода _CRS/_SUB/_DEP: каждый вернул объект. Список resource-evaluation.json и resources-batch0..8.log. Это проверка выполнения, а не соответствия возвращённых ресурсов железу и не полный перебор условий. Повторяются только ранее зафиксированные GPU0.AVS0 при загрузке namespace и4allocations инструмента на выходе. Статические объекты Name(_DEP/...) в число72методов не входят.

Диагностическое ядро для будущего чтения таблиц: взять рабочий DT audio2, выключить только STRICT_DEVMEM и зависимую IO_STRICT_DEVMEM (если включена), сохранить DEVMEM=y, конфигурацию USB/сети и весь рабочий initramfs. При новом LOCALVERSION обязательно обеспечить соответствующие модули и проверить привязанные к версии сценарии звука/графики; простая смена суффикса сломает существующие проверки. Безопаснее подготовить отдельный Image/пункт с документированным hash, проверив совместимость модулей, прежде чем выбирать вариант. Ничего из этого пока не собрано/не установлено, перезагрузка не выполнялась.
